# CHZZK All-in-One QA 체크노트

- 대상 버전: `1.0.2`
- 우선 브라우저: Firefox 142 이상
- 주 개발 환경: Windows 10/11
- 결과 표기: `[ ]` 미실행 · `[x]` 통과 · `[!]` 실패 · `[-]` 해당 없음/차단

## 0. 현재 동작과 범위

| 항목 | 현재 상태 |
|---|---|
| 실시간 트렌드 자동 갱신 | 기본 1분. 팝업에서 1~60분으로 변경 가능 |
| 트렌드 즉시 갱신 | 트렌드 바의 시각/`↻` 버튼 클릭 |
| 급상승 판정 | 직전 조회보다 `500명 이상` 또는 `20% 이상` 증가 |
| 팔로잉 채널 방송 시작 알림 | 활성 CHZZK 탭의 팔로잉 사이드바 DOM 변경을 즉시 감지하고 5초마다 보조 확인. Firefox 시스템 알림이 아닌 페이지 내 카드 UI |
| 팔로우 통나무 자동 획득 | 구현됨. 사용자가 채널을 팔로우한 직후 보상 claim을 확인하는 기능이며 방송 시작 알림과는 별개 |
| Chromium | 후순위. 이번 QA의 합격 대상이 아님 |

## 1. 테스트 기록 양식

각 실패 항목에 아래 정보를 남긴다.

```text
QA ID:
결과: PASS / FAIL / BLOCKED
Firefox 버전:
Windows 버전:
CHZZK URL / 채널 ID:
로그인 상태:
재현 순서:
실제 결과:
기대 결과:
콘솔 오류:
스크린샷/녹화 파일:
발생 시각:
```

심각도 기준:

- P0: 데이터 손실, 브라우저/사이트 사용 불가, 보안 문제
- P1: 광고 제거·광고 경고 제거·1080p·GRID/P2P 우회 등 핵심 기능 실패
- P2: 일부 조건 실패, 설정 미반영, 중복 UI
- P3: 정렬·색상·문구 등 시각/사용성 문제

## 2. 사전 준비

- [ ] ENV-01 Firefox 버전이 142 이상이다.
- [ ] ENV-02 기존 Cheese-PIP, 트렌드 유저스크립트, GRID 우회 스크립트, 통나무 확장을 모두 끈다.
- [ ] ENV-03 `about:debugging#/runtime/this-firefox`에서 이 확장 하나만 로드한다.
- [ ] ENV-04 CHZZK에 로그인되어 있고 성인/연령 제한 방송도 별도로 확인할 수 있다.
- [ ] ENV-05 소리가 있는 라이브 방송, VOD, 클립 URL을 각각 준비한다.
- [ ] ENV-06 테스트용으로 팔로우하지 않은 라이브 채널과 통나무 활성 채널을 준비한다.
- [ ] ENV-07 Firefox 개발자 도구의 Console과 Network 탭을 열 수 있다.
- [ ] ENV-08 다운로드 폴더에 충분한 공간이 있다. MP4 호환 변환은 원본보다 큰 임시 메모리를 사용할 수 있다.
- [ ] ENV-09 일반 프로필과 새 Firefox 프로필에서 각각 한 번 실행한다.

## 3. 자동 검사와 패키지

- [ ] AUTO-01 `npm install`이 오류 없이 끝난다.
- [x] AUTO-02 `npm test` 결과가 19 tests / 19 pass이다.
- [x] AUTO-03 `npx web-ext lint --source-dir .` 결과가 errors 0이다.
- [x] AUTO-04 CSP 경고 1개만 존재한다. 로컬 ffmpeg WebAssembly 실행용 `wasm-unsafe-eval` 검토 경고이다.
- [x] AUTO-05 `./build.ps1` 실행 후 동일 버전의 `.zip`과 `.xpi`가 생성된다.
- [x] AUTO-06 XPI 루트에 `manifest.json`이 있고 `record-result.*`, `vendor/ffmpeg/**`, `icons/**`, `LICENSE`, `NOTICE`가 포함된다.
- [x] AUTO-07 `node_modules`, `test`, `assets`, 구버전 `dist` 파일은 XPI에 들어가지 않는다.

## 4. 설치·메타데이터·팝업

- [ ] META-01 확장 이름이 `CHZZK All-in-One by hanbi`로 표시된다.
- [ ] META-02 버전, author `hanbi`, 설명, 아이콘 16/32/48/96/128px가 정상이다.
- [ ] META-03 팝업 상단 아이콘이 찌그러지거나 잘리지 않는다.
- [ ] META-04 `NOTICE`에서 Cheese-PIP, 통나무, GRID, 트렌드, ffmpeg 출처를 확인할 수 있다.
- [ ] META-05 팝업을 닫았다 다시 열어도 모든 토글과 숫자 설정이 유지된다.
- [ ] META-06 CHZZK가 아닌 탭에서 팝업 설정을 바꿔도 오류가 발생하지 않는다.
- [ ] META-07 일반 Firefox에서 미서명 XPI 영구 설치가 거부되는 것은 예상 동작이다. 배포본은 AMO 서명 여부를 별도 확인한다.

## 5. GRID/P2P 우회와 최고 화질

- [ ] NET-01 Windows에서 `GRID 우회`가 기본 ON이다.
- [ ] NET-02 토글 OFF 후 DNR ruleset `grid_bypass`가 비활성화된다.
- [ ] NET-03 토글 ON 후 ruleset이 다시 활성화된다.
- [ ] NET-04 480p HLS 요청이 발생하면 대응되는 1080p URL로 리다이렉트된다.
- [ ] NET-05 HLS master playlist에서 가장 높은 1080p variant 하나가 선택된다.
- [ ] NET-06 API JSON의 `p2pQuality`가 빈 배열이 되고 `meta.p2p`가 false가 된다.
- [ ] NET-07 `p2pPath`, `p2pPathUrlEncoding` 필드가 제거된다.
- [ ] NET-08 JSON이 아닌 응답, 깨진 JSON, unrelated API는 변경되지 않는다.
- [ ] NET-09 치트키 제한 스포츠 중계에 1080p가 없으면 서버가 제공한 480p/360p 중 최고 화질로 계속 재생된다.

### 핵심 4종 출시 차단 게이트

- [ ] CORE-01 SSAI 광고용 `.mp4`/`/service/` 미디어만 제거되고 실제 HLS 방송은 재생된다.
- [ ] CORE-02 `광고 차단 프로그램 사용 중` 경고만 제거되고 unrelated dialog는 유지된다.
- [ ] CORE-03 1080p 요청 리다이렉트와 master playlist 1080p 선택이 모두 동작한다.
- [ ] CORE-04 `dab`, `p2pQuality`, `livePlaybackJson.meta.p2p`, 두 `p2pPath` 필드가 모두 우회된다.
- [ ] CORE-05 위 네 기능 중 하나라도 실패하면 P1 및 출시 차단으로 기록한다.
- [ ] NET-09 최고 화질이 없는 방송에서도 재생이 중단되지 않는다.
- [ ] NET-10 라이브 채널 간 SPA 이동 후에도 우회와 화질 선택이 유지된다.
- [ ] NET-11 네트워크 오류 또는 CDN 변경 시 무한 새로고침/요청 루프가 없다.

## 6. 플레이어 공통 UI

- [ ] UI-01 라이브 플레이어 오른쪽 하단에 기능별 `REC`, `SHOT`, 조건부 `PIP` 버튼이 한 세트만 생긴다.
- [ ] UI-02 플레이어 DOM이 다시 그려져도 버튼 세트가 중복 생성되지 않는다.
- [ ] UI-03 각 기능 토글 OFF 시 해당 버튼이 즉시 사라지고 ON 시 다시 생긴다.
- [ ] UI-04 버튼 tooltip/title/aria-label이 키보드와 스크린리더에서 식별 가능하다.
- [ ] UI-05 전체 화면, 창 크기 변경, 극장 모드에서도 버튼이 플레이어 조작을 막지 않는다.
- [ ] UI-06 오류 toast가 한 개만 표시되고 약 3.5초 후 사라진다.
- [ ] UI-07 확장 팝업·로그·녹화 결과 화면은 검정/흰색/회색만 사용한다.
- [ ] UI-08 CHZZK 페이지 위 UI는 검정/회색 바탕에 CHZZK 초록색 강조 하나만 사용한다.
- [ ] UI-09 전환 애니메이션은 짧은 `steps()` 방식이며 끊김이 의도된 동작처럼 일정하다.

## 7. 스크린샷

- [ ] SHOT-01 `SHOT` 클릭 시 실제 영상 해상도의 PNG가 생성된다.
- [ ] SHOT-02 CORS 때문에 canvas 캡처가 막히면 현재 탭 화면을 캡처한 뒤 영상 영역만 정확히 자른다.
- [ ] SHOT-03 캡처 중 확장 버튼 자체는 결과 이미지에 들어가지 않는다.
- [ ] SHOT-04 미리보기는 `#hanbi-screenshot-preview` 하나만 존재한다.
- [ ] SHOT-05 연속 촬영 시 기존 미리보기 이미지가 교체되고 두 개로 늘어나지 않는다.
- [ ] SHOT-06 제목 바를 드래그하면 팝업이 이동하며 화면 밖으로 완전히 사라지지 않는다.
- [ ] SHOT-07 저장 버튼으로 올바른 PNG와 안전한 파일명이 내려받아진다.
- [ ] SHOT-08 닫기 버튼으로 팝업이 즉시 제거된다.
- [ ] SHOT-09 16:9가 아닌 영상도 찌그러지지 않고 검은 여백으로 표시된다.
- [ ] SHOT-10 영상이 화면 밖/숨김/재생 전일 때 명확한 오류가 나오고 빈 이미지가 저장되지 않는다.

## 8. 녹화 중 동작

- [ ] REC-01 `REC` 클릭 후 버튼이 `STOP`으로 바뀌고 초록 강조 녹화 타이머가 한 개만 표시된다.
- [ ] REC-02 타이머가 `00:00`부터 매초 증가한다.
- [ ] REC-03 녹화 중 방송 영상이 계속 보인다.
- [ ] REC-04 **Firefox에서 녹화 중 방송 소리가 계속 들린다.**
- [ ] REC-05 플레이어 볼륨 변경이 녹화 중 청취 음량에 즉시 반영된다.
- [ ] REC-06 음소거하면 모니터 소리가 0이 되고 해제하면 돌아온다.
- [ ] REC-07 녹화 결과 파일에도 영상과 오디오가 모두 존재하고 싱크가 맞는다.
- [ ] REC-08 오디오 트랙이 없는 방송은 `오디오 없이` 안내 후 영상만 정상 녹화한다.
- [ ] REC-09 `STOP` 클릭 후 버튼과 타이머가 원래 상태로 복구된다.
- [ ] REC-10 녹화 종료 시 별도 결과 탭이 한 개 열린다.
- [ ] REC-11 결과 탭 전달 실패 시 원본 WebM이 자동 저장되어 데이터가 유실되지 않는다.
- [ ] REC-12 10초, 10분, 1시간 녹화에서 메모리 폭증·탭 멈춤·빈 파일이 없는지 기록한다.
- [ ] REC-13 방송 종료, 채널 이동, 탭 닫기, 네트워크 끊김 도중 녹화 중지 동작을 확인한다.
- [ ] REC-14 두 라이브 탭에서 동시에 녹화를 시도한다. 현재 설계는 pending 결과 1개 제한이므로 덮어쓰기 여부를 P1로 기록한다.

## 9. 녹화 결과·변환

- [ ] CONV-01 결과 탭에 영상 미리보기, 파일명, 재생 시간, 용량, MIME이 표시된다.
- [ ] CONV-02 원본 저장이 즉시 실행되고 WebM 파일이 재생된다.
- [ ] CONV-03 `MP4 빠른 변환`이 화질 손실 없이 완료된다.
- [ ] CONV-04 빠른 변환이 코덱 문제로 실패하면 오류 안내가 나오며 원본은 유지된다.
- [ ] CONV-05 `MP4 호환 변환 · 추천` 결과가 H.264/AAC이며 Firefox 외 플레이어에서도 재생된다.
- [ ] CONV-06 GIF 결과가 640px/15fps, 무음, 반복 재생된다.
- [ ] CONV-07 WebP 결과가 640px/15fps, 무음, 반복 재생된다.
- [ ] CONV-08 자르기 시작 0/종료 N초 결과 길이가 예상 범위다.
- [ ] CONV-09 종료 시간이 시작보다 작거나 같으면 변환하지 않고 검증 오류를 표시한다.
- [ ] CONV-10 분할 간격 1초 및 600초에서 `001`, `002` 순번 파일이 빠짐없이 저장된다.
- [ ] CONV-11 변환 중 모든 실행 버튼이 잠기고 진행률·상태가 표시된다.
- [ ] CONV-12 변환 완료/실패 후 버튼이 다시 활성화된다.
- [ ] CONV-13 변환은 외부 업로드 없이 로컬에서만 처리된다.
- [ ] CONV-14 ffmpeg 약 32MB 코어는 변환 버튼을 누르기 전에는 로드되지 않는다.
- [ ] CONV-15 변환 중 결과 탭을 닫아도 다른 CHZZK 탭이나 다음 녹화가 깨지지 않는다.
- [ ] CONV-16 파일명에서 `\\ / : * ? \" < > |` 문자가 안전하게 치환된다.

## 10. Picture-in-Picture

- [ ] PIP-01 브라우저/영상이 표준 PIP API를 지원할 때만 `PIP` 버튼이 보인다.
- [ ] PIP-02 진입과 종료이 정상이며 소리·재생 상태가 유지된다.
- [ ] PIP-03 브라우저가 API를 지원하지 않으면 버튼이 숨겨지고 다른 버튼에는 영향이 없다.
- [ ] PIP-04 미리보기용/숨겨진 video가 아니라 화면에서 가장 큰 실제 방송 video가 선택된다.

## 11. 자동 음소거 해제·광고 안내 제거

- [ ] BASIC-01 라이브 최초 로드 시 `자동 음소거 해제` ON이면 영상 muted가 false가 된다.
- [ ] BASIC-02 `loadeddata` 후에도 한 번 더 음소거가 해제된다.
- [ ] BASIC-03 토글 OFF에서는 사용자의 음소거 상태를 강제로 바꾸지 않는다.
- [ ] BASIC-04 `광고 차단 프로그램 사용 중` 안내 팝업만 제거된다.
- [ ] BASIC-04A 새 `_modal_* _container_*` 구조의 `광고 차단 프로그램을 사용 중이신가요?` 모달도 화면에 남지 않는다.
- [ ] BASIC-04B `치트키를 구매하면 타임머신 기능을 이용할 수 있어요!` 구매 안내 모달도 화면에 남지 않는다.
- [ ] BASIC-05 설정, 로그인, 연령 확인 등 unrelated dialog는 제거되지 않는다.
- [ ] BASIC-06 광고 안내 토글 OFF에서는 해당 팝업을 건드리지 않는다.

## 12. 블라인드 채팅 복구

- [ ] CHAT-01 정상 채팅은 원문을 캐시에 기억하되 화면은 변경하지 않는다.
- [ ] CHAT-02 같은 DOM 항목이 관리자/운영자 블라인드 문구로 바뀌면 캐시 원문이 표시된다.
- [ ] CHAT-03 DOM 안에 숨겨진 원문 노드가 있으면 그 원문을 우선 표시한다.
- [ ] CHAT-04 복구 원문은 취소선/낮은 불투명도로 표시되고 중복 생성되지 않는다.
- [ ] CHAT-05 `블라인드 테스트 방송입니다` 같은 일반 문장은 블라인드 공지로 오인하지 않는다.
- [ ] CHAT-06 채팅 항목 DOM이 재사용될 때 다른 사용자의 이전 메시지를 잘못 붙이지 않는다.
- [ ] CHAT-07 토글 OFF 시 확장 복구 문구와 마스킹 속성이 즉시 제거된다.
- [ ] CHAT-08 빠른 채팅, 채팅 스크롤, 채팅 패널 닫기/열기에서 CPU 사용이 과도하지 않다.

## 13. 실시간 트렌드

- [ ] TREND-01 기능 ON 시 헤더 바로 아래 트렌드 바가 한 개만 생성된다.
- [ ] TREND-02 최초 진입 즉시 `size=50&sortType=POPULAR` API를 호출한다.
- [ ] TREND-03 기본 설정은 기준 1,000명, 표시 10개, 갱신 1분이다.
- [ ] TREND-04 팝업에서 기준 시청자 0 이상, 표시 수 1~30, 갱신 1~60분 범위가 적용된다.
- [ ] TREND-05 저장한 숫자 설정이 새로고침과 브라우저 재시작 후 유지된다.
- [ ] TREND-06 자동 갱신이 설정 주기대로 동작하고 동시에 중복 요청하지 않는다.
- [ ] TREND-07 시각/`↻` 버튼을 클릭하면 즉시 갱신된다.
- [ ] TREND-08 갱신 시각이 24시간 형식으로 바뀐다.
- [ ] TREND-09 첫 조회는 인기 상위 목록을 표시하고 급상승 표시를 붙이지 않는다.
- [ ] TREND-10 다음 조회부터 기준 시청자 이상 방송을 대상으로 급상승을 계산한다.
- [ ] TREND-11 직전보다 500명 이상 또는 20% 이상 증가하면 `⚡`와 짧은 단계식 강조가 표시된다.
- [ ] TREND-12 기준을 만족하는 방송이 없을 때 인기 상위 목록으로 fallback한다.
- [ ] TREND-13 링크의 순위, 채널명, 축약 시청자 수가 정확하다.
- [ ] TREND-14 링크 hover 시 720 타입 썸네일, 방송 제목, 채널, 정확한 시청자 수가 하나의 팝업에 표시된다.
- [ ] TREND-15 hover 종료 시 썸네일 팝업이 사라지고 화면 밖으로 넘어가지 않는다.
- [ ] TREND-16 방송 링크 클릭 시 해당 `/live/{channelId}`로 이동한다.
- [ ] TREND-17 light/dark 테마, 760px 이하 폭, 가로 스크롤에서 글자와 버튼이 겹치지 않는다.
- [ ] TREND-18 API 오류·빈 배열일 때 기존 사이트 헤더가 깨지거나 무한 재시도하지 않는다.
- [ ] TREND-19 토글 OFF 시 트렌드 바와 썸네일 팝업이 모두 제거된다.
- [ ] TREND-20 SPA 이동이나 CHZZK 헤더 재생성 후 트렌드 바가 한 번만 복구된다.
- [ ] TREND-21 왼쪽 메뉴가 열리거나 접힐 때 실제 메뉴 폭만큼 트렌드 바 시작점과 폭이 즉시 보정된다.

## 14. 통나무 파워 기본

- [ ] POWER-01 라이브 진입 2초 내 초기 잔액 조회가 실행된다.
- [ ] POWER-02 잔액은 30초마다 갱신되고 DOM 배지/시계/받기 버튼은 1초마다 확인된다.
- [ ] POWER-03 `파워 표시` ON/OFF가 채팅 입력 영역 배지에 즉시 반영된다.
- [ ] POWER-04 배지를 클릭하면 전체 채널 잔액 팝업이 열리고 프로필·채널명·수량이 표시된다.
- [ ] POWER-05 비활성 채널은 배지 비활성 스타일과 tooltip을 표시한다.
- [ ] POWER-06 `개수 요약` OFF는 전체 숫자, ON은 축약 숫자를 표시한다.
- [ ] POWER-07 `예상 획득 시간` ON 시 마지막 획득 로그 기준 다음 시간까지 남은 시간이 표시된다.
- [ ] POWER-08 `움직이는 GIF 프로필` ON 시 프로필 URL의 `_na` 타입만 제거되고 unrelated URL은 바뀌지 않는다.
- [ ] POWER-09 라이브 채널 SPA 이동 후 이전 채널 상태가 남지 않고 새 채널 잔액으로 바뀐다.
- [ ] POWER-10 백그라운드 탭에서도 visibility/focus 우회로 시청 판정이 유지된다.
- [ ] POWER-11 visibility/focus 오버라이드가 채팅, 키보드, PIP, 플레이어 단축키를 망가뜨리지 않는지 회귀 검사한다.

## 15. 통나무 자동 획득

- [ ] CLAIM-01 1시간 시청 `받기` 버튼이 나타나면 1초 내 자동 클릭된다.
- [ ] CLAIM-02 최근 1분 안에 동일 view 로그가 있으면 중복 로그를 만들지 않는다.
- [ ] CLAIM-03 비구독 100, 1티어 120, 2티어 200 수량이 로그에 맞게 기록된다.
- [ ] CLAIM-04 API claim 중 `WATCH_1_HOUR`는 PUT하지 않고 화면 버튼 경로만 사용한다.
- [ ] CLAIM-05 FOLLOW 등 나머지 claim은 PUT 후 잔액과 로그가 갱신된다.
- [ ] CLAIM-06 사용자가 팔로우하면 follow API를 감지하고 5초 간격으로 claim/잔액을 확인한다.
- [ ] CLAIM-07 FOLLOW 보상이 들어오면 300 또는 서버 반환량으로 한 번만 기록된다.
- [ ] CLAIM-08 follow 보상이 없거나 API가 실패할 때 5초 폴러가 무기한 남는지 장기 관찰한다. 남으면 P2 버그로 기록한다.
- [ ] CLAIM-09 로그아웃, 권한 오류, 비활성 채널에서 자동 PUT/클릭 루프가 발생하지 않는다.
- [ ] CLAIM-10 여러 라이브 탭을 동시에 열었을 때 중복 claim/중복 로그가 생기지 않는다.

## 16. 승부예측 추적

- [ ] PRED-01 예측 참여 요청을 PerformanceObserver가 감지한다.
- [ ] PRED-02 서버 처리 대기 3초 후 선택 옵션과 배팅 파워를 가져온다.
- [ ] PRED-03 참여 시 배팅 파워가 음수 prediction 로그로 한 번 기록된다.
- [ ] PRED-04 같은 predictionId 재참여/수정 시 이전 음수 로그가 교체된다.
- [ ] PRED-05 pending 예측 상태를 30초마다 확인한다.
- [ ] PRED-06 COMPLETED 승리 시 로그 금액과 상세 정보가 최종 수익/서버 winningPowers에 맞게 갱신된다.
- [ ] PRED-07 패배 시 결과와 선택 옵션이 정확히 표시된다.
- [ ] PRED-08 CANCELLED 시 배팅 음수 로그가 제거되고 잔액이 재조회된다.
- [ ] PRED-09 브라우저/확장 재시작 후 pending 예측 추적이 재개된다.
- [ ] PRED-10 EXPIRED/CANCELLED/COMPLETED 상세 동기화가 30초 내 완료된다.

## 17. 통나무 로그 화면

- [ ] LOG-01 `통나무 로그 보기`가 별도 확장 탭을 연다.
- [ ] LOG-02 오늘/이번 주/이번 달/전체의 횟수·통나무·채널 수가 실제 로그 합계와 같다.
- [ ] LOG-03 `승부예측 별도 집계` ON/OFF에 따라 기간 집계가 바뀐다.
- [ ] LOG-04 방식(팔로우/시청/승부예측/기타), 채널명, 날짜 필터가 조합되어 동작한다.
- [ ] LOG-05 로그 카드에서 채널, 시각, 수량, 획득 방식이 정확하다.
- [ ] LOG-06 승부예측 상세 버튼이 활성 CHZZK 탭을 통해 데이터를 가져온다.
- [ ] LOG-07 개별 로그 편집·저장·취소·삭제가 올바른 원본 항목에 적용된다.
- [ ] LOG-08 전체 로그 삭제는 확인 후 실행되며 취소하면 보존된다.
- [ ] LOG-09 테스트 로그 추가 기능으로 각 방식과 양수/음수 수량을 만들 수 있다.
- [ ] LOG-10 기본 최대 저장 수는 10,000개이고 변경값이 sync 저장소에 유지된다.
- [ ] LOG-11 최대치를 넘으면 오래된 로그부터 제거된다.
- [ ] LOG-12 JSON 내보내기 파일을 다시 가져오면 원본 데이터가 보존된다.
- [ ] LOG-13 잘못된 JSON, 배열이 아닌 JSON, 악성 HTML 문자열을 가져와도 실행되지 않고 안전하게 실패한다.
- [ ] LOG-14 dark/light 테마가 유지되고 모바일 폭에서도 카드/모달이 잘리지 않는다.

## 18. 저장소·권한·개인정보

- [ ] SAFE-01 manifest 권한이 storage/downloads/activeTab/tabs/DNR과 필요한 CHZZK/CDN host로 제한된다.
- [ ] SAFE-02 외부 분석·광고·추적 서버 요청이 없다.
- [ ] SAFE-03 녹화 변환 중 ffmpeg 코어 외 원격 JS/WASM을 내려받지 않는다.
- [ ] SAFE-04 설정과 통나무 로그는 Firefox local/sync 저장소에만 남는다.
- [ ] SAFE-05 결과 파일과 스크린샷은 사용자가 실행한 경우에만 다운로드된다.
- [ ] SAFE-06 로그의 채널명·이미지 URL·가져온 문자열이 HTML/스크립트로 실행되지 않는다.
- [ ] SAFE-07 시크릿 창 권한을 허용하지 않은 경우 시크릿 탭에 확장이 주입되지 않는다.

## 19. 안정성·중복·장시간 회귀

- [ ] STAB-01 라이브 3개, VOD, 홈을 30회 SPA 이동해도 확장 UI 개수가 늘지 않는다.
- [ ] STAB-02 `#hanbi-player-tools`, `#hanbi-screenshot-preview`, `#hanbi-trends`, `#hanbi-trend-preview`, 파워 배지는 각각 최대 하나다.
- [ ] STAB-03 2시간 시청 후 MutationObserver, 1초/30초/1분 타이머가 중복 실행되지 않는다.
- [ ] STAB-04 Firefox 작업 관리자에서 CPU·메모리 증가가 계속 누적되지 않는다.
- [ ] STAB-05 네트워크 오프라인→온라인 복귀 후 플레이어·트렌드·파워가 회복된다.
- [ ] STAB-06 확장 비활성화/재활성화 후 페이지 새로고침으로 모든 DOM 패치가 정상화된다.
- [ ] STAB-07 light/dark 전환 후 색상 대비와 아이콘 가독성을 확인한다.
- [ ] STAB-08 125%/150% Windows 배율과 Firefox 80%/100%/150% 확대에서 UI가 겹치지 않는다.

## 20. 팔로잉 채널 방송 시작 웹 팝업

- [ ] FOLLOW-NOTI-01 `notifications` 권한 없이 사용자 ON/OFF 설정을 제공한다.
- [ ] FOLLOW-NOTI-02 열린 팔로잉 사이드바의 라이브 링크를 읽고 채널 ID·이름·썸네일을 수집한다.
- [ ] FOLLOW-NOTI-03 OFFLINE→OPEN 상태 전환만 알림으로 판단한다.
- [ ] FOLLOW-NOTI-04 확장 시작 직후 이미 방송 중인 모든 채널을 한꺼번에 알리지 않는다.
- [ ] FOLLOW-NOTI-05 같은 채널이 계속 라이브인 동안 한 번만 알리고, 오프라인 후 재시작하면 다시 알린다.
- [ ] FOLLOW-NOTI-06 페이지 오른쪽 위 직접 제작한 카드에 LIVE, 채널명, 제목, 썸네일을 표시한다.
- [ ] FOLLOW-NOTI-07 `방송 보기`를 누르면 해당 `/live/{channelId}`로 이동한다.
- [ ] FOLLOW-NOTI-08 방송 재시작/제목 변경/카테고리 변경을 중복 시작 알림으로 오인하지 않는다.
- [ ] FOLLOW-NOTI-09 팔로잉 API 404와 무관하게 사이드바 DOM만으로 동작하며 기존 페이지 UI를 깨뜨리지 않는다.
- [ ] FOLLOW-NOTI-10 활성 CHZZK 탭의 DOM 변경 직후와 5초 보조 주기에 확인하고 마지막 상태를 local 저장한다.
- [ ] FOLLOW-NOTI-11 카드가 15초 후 자동 종료되고, 닫기 버튼이 동작하며, 최대 3개만 표시된다.
- [ ] FOLLOW-NOTI-12 확장 설정의 `방송 시작 알림 테스트`를 누르면 카드와 팔로잉 사이드바 감지 여부·방송 수가 함께 확인된다.
- [ ] FOLLOW-NOTI-13 기능 OFF 시 조회와 표시가 멈추고 기존 카드가 제거된다.
- [ ] FOLLOW-NOTI-14 모바일·light/dark 테마에서 카드가 화면 밖으로 잘리지 않는다.
- [ ] FOLLOW-NOTI-15 인기 카테고리·파트너 스트리머·방송 일정의 라이브 링크는 팔로잉 알림으로 수집되지 않는다.

## 21. 출시 판정

- [ ] RELEASE-01 P0/P1 미해결 0개
- [ ] RELEASE-02 P2는 원인·우회법·수정 버전이 체크노트에 기록됨
- [ ] RELEASE-03 자동 검사 전부 통과
- [ ] RELEASE-04 핵심 4종, REC+오디오, MP4 호환 변환, 스크린샷, 트렌드, 팔로잉 팝업, 블라인드, 통나무 자동 획득 실사용 통과
- [ ] RELEASE-05 새 프로필과 기존 사용자 프로필에서 각각 통과
- [ ] RELEASE-06 버전·README·NOTICE·XPI 파일명이 일치
- [ ] RELEASE-07 AMO 제출본이면 서명·데이터 수집 선언·CSP 검토 사유 확인

## 22. 추가 플레이어·탐색 기능

- [ ] EXT-01 플레이어 볼륨 옆 `COMP`를 켜면 원본 cheese-knife와 같은 `MediaElementSource → DynamicsCompressor → Gain` 그래프로 연결되고, 끄면 원본 출력으로 우회한다.
- [ ] EXT-01A 기본값이 threshold -50dB, knee 40dB, ratio 12:1, attack 0초, release 0.25초이며 Gain 슬라이더가 0~200%로 동작한다.
- [ ] EXT-02 `영상 필터 (밝기/대비)` 수치 조절 시 실시간 영상에 즉시 반영된다.
- [ ] EXT-03 `방향키 탐색` 활성화 시 좌/우 방향키로 5초씩 영상이 이동한다 (타임머신).
- [ ] EXT-04 채팅창 입력 중에 방향키를 누르면 영상이 이동하지 않고 커서만 이동한다.
- [ ] EXT-05 `치즈 후원 채팅 숨기기` 활성화 시 후원 메시지가 채팅창에서 보이지 않는다.
- [ ] EXT-06 `채팅창 글자 크기`를 변경하면 기존/새 채팅 메시지 폰트 크기가 변경된다.
- [ ] EXT-07 `사이드바 자동 갱신` 활성화 시 좌측 팔로잉 탭이 30초마다 자동으로 갱신된다.
- [ ] EXT-08 `마우스 오버 미리보기` 활성화 시 사이드바 방송 링크에 마우스를 올리면 썸네일 팝업이 뜬다.
- [ ] EXT-09 썸네일 팝업이 화면 밖으로 나가지 않으며 마우스를 떼면 즉시 사라진다.

## 23. 최초 소스·현재 구현 대조표

| 최초 소스/참고 | 원래 기능 | 현재 위치 | 확인 |
|---|---|---|---|
| 사용자 제공 P2P Bypass | `p2pQuality`, playback P2P 경로 제거 | `page.js` | 자동 테스트 |
| 사용자 제공 올인원 스크립트 | 최고 화질, 광고 경고 제거, 자동 음소거 해제 | `page.js`, `tools.js` | 자동+수동 |
| 사용자 제공 1080p 개선판·Best Quality | master playlist 최고 1080p 선택 | `page.js` | 자동 테스트 |
| 사용자 제공 블라인드 복구 | 확인 가능한 원문 보존·복구 | `tools.js` | 자동+수동 |
| 사용자 제공 트렌드 2.1 | 인기/급상승, 갱신, 썸네일 미리보기 | `tools.js` | 자동+수동 |
| 사용자 제공 SSAI Ad Blocker | 라이브 광고 미디어 건너뛰기 | `page.js` | 자동+수동 |
| Cheese-PIP | REC, SHOT, 방향키 탐색, PIP | `tools.js`, `record-result.*` | 자동+수동 |
| chzzk_auto_log_power | 자동 획득, 잔액, 랭킹, 로그, 승부예측 | `content.js`, `log.*` | 원본 대조+수동 |
| FUCK-CHZZK-GRID | 480p 요청을 1080p로 치환, ruleset ON/OFF | `rules.json`, `background.js` | 자동+수동 |
| cheese-knife | 오디오 컴프레서 그래프·기본값·Gain | `tools.js` | 자동+수동 |
| Better Chzzk 동작 참고 | 블라인드·사이드바 갱신·호버 미리보기 | `tools.js` 독립 구현 | 수동 |
| v1.0.0 후속 타임머신 패치 | 응답의 기존 `timeMachine` 플래그 보존, 구매 안내 숨김 | `page.js`, `tools.js` | 자동+수동 |

타임머신은 최초 첨부 스크립트나 세 GitHub 원본에는 없고 `a0f9b4d`에서 추가됐다. 현재 코드는 그 패치를 삭제하지 않으며, 방향키는 브라우저가 실제로 제공한 seekable 범위 안에서만 이동한다.
